---
title: Slider
description: Slider is a component that allows the user to select a value from a range of values.
---

import SliderDemo from 'registry/ui3/examples/slider-demo';
import ColorRangeSliderDemo from 'registry/ui3/examples/color-range-slider-demo';
import OpacitySliderDemo from 'registry/ui3/examples/opacity-slider-demo';
import sliderDemoSource from 'registry/ui3/examples/slider-demo.tsx?raw';
import colorRangeSliderDemoSource from 'registry/ui3/examples/color-range-slider-demo.tsx?raw';
import opacitySliderDemoSource from 'registry/ui3/examples/opacity-slider-demo.tsx?raw';
import sliderSource from 'registry/ui3/ui/slider.tsx?raw';

<ComponentTabs>
  <ComponentPreview name='slider-demo'>
    <SliderDemo />
  </ComponentPreview>
  <ComponentSource code={sliderDemoSource} language='tsx' title='slider-demo.tsx' />
</ComponentTabs>

## Example

Up to now, we have three types of sliders:

- Default Slider
- Color Range Slider
- Opacity Slider

You need to install `chroma-js` to use the color range slider:

```bash
pnpm add chroma-js
```

### Color Range Slider

Color range slider is a slider that allows the user to select a color (hue value of HSL color space, more specifically) from a range of colors. The slider value is a number between `0` and `360`.

<ComponentTabs>
  <ComponentPreview name='color-range-slider-demo'>
    <ColorRangeSliderDemo />
  </ComponentPreview>
  <ComponentSource code={colorRangeSliderDemoSource} language='tsx' title='color-range-slider-demo.tsx' />
</ComponentTabs>

### Opacity Slider

Opacity slider is a slider that allows the user to select a value between `0` and `1`. And accept a `color` string as a prop.

<ComponentTabs>
  <ComponentPreview name='opacity-slider-demo'>
    <OpacitySliderDemo />
  </ComponentPreview>
  <ComponentSource code={opacitySliderDemoSource} language='tsx' title='opacity-slider-demo.tsx' />
</ComponentTabs>

## Source

<ComponentSource code={sliderSource} language='tsx' title='slider.tsx' />
