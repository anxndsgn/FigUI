---
title: Color Input
---

import ColorInputDemo from '@/registry/ui3/examples/color-input-demo';
import { ColorInput } from '@/registry/ui3/ui/input';
import { ComponentSource } from '@/components/component-source';

A text input that validates hex colors. Invalid input falls back to the last valid value on blur / Enter, and valid input is normalized to uppercase 6-digit hex (no leading `#`).

<ComponentPreview
  name='color-input'
  src='registry/ui3/examples/color-input-demo.tsx'
  component={<ColorInputDemo />}
  description='A color input'
/>

## Manual Installation

Make sure to install `chroma-js`.

<ComponentSource
  src='registry/ui3/ui/input/color-input.tsx'
  title='input/color-input.tsx'
  language='tsx'
/>
